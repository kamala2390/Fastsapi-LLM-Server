# FastAPI LLM Server

A simple FastAPI server containerized with Docker.

# To Run locally:

```bash
docker build -t fastapi-llm-server .
docker run -p 8000:8000 fastapi-llm-server
```

 visit http://localhost:8000